cat > render.yaml << EOF
services:
- type: web
 name: mental-health-signal
 runtime: python
 plan: free
 buildCommand: pip install -r requirements.txt
 startCommand: uvicorn main:app --port \$PORT
 envVars:
 - key: PORT
 value: 2200
EOF
